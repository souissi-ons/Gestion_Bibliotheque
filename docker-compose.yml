version: '3.8'

services:
  library-api:
    build: .
    image: library-api:latest
    container_name: library-api
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    restart: unless-stopped
